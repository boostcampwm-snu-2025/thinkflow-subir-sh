# thinkflow-lsh
아이디어(메모, 포스팅) + 태스크(투두 리스트, 캘린더)를 한 번에 관리할 수 있는 문서 정리 앱 (w/ LLM)

Docker desktop 실행 후, docker compose up -d

FE -> localhost:5173

BE -> localhost:3000

(Swagger -> localhost:3000/api-docs)